import createElement from '../utils/create-element';

export default class GetError {
  element: HTMLElement = createElement('h2', 'error wrapper', '404 page no found');
  constructor() {
    this.element;
  }
}
